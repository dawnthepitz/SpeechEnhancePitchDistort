How to use the VoiceChange Function

VoiceChange(wavFile,gender)

wavFile=file name and path(if outside the folder of the VoiceChange) of the wavfile
	ex: 'wavfile.wav'
gender=a desired gender for the voice distortion
	0=male
	1=female